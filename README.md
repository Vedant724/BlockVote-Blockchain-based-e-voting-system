# Blockchain E-Voting Frontend

Premium UI scaffold for a blockchain-based e-voting system.

## Run

1. `npm install`
2. `npm start`

This version is UI-only and does not call backend endpoints yet.
